# MLOPs-A4 Project

## Overview
This repository contains a simple Generative Adversarial Network (GAN) trained on the MNIST dataset using PyTorch.

## Setup
Install the dependencies using `pip install -r requirements.txt`.

## Running the Code
Run `python gan_mnist.py` to train the GAN model.
